name: PR test

on:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: run script
        run: bash x.sh
        env:
          TOKEN: ${{ secrets.DEMO_TOKEN }}
